package main

/*import "fmt"

func main() {
	matrix1 := [2][2]int{}
	for i := 0; i < 2; i++ {
		for j := 0; j < 2; j++ {
			var input int
			fmt.Println("matrix1[", i, "][", j, "] = ")
			fmt.Scanln(&input)
			matrix1[i][j] = input
		}
	}
	matrix2 := [2][2]int{}
	for i := 0; i < 2; i++ {
		for j := 0; j < 2; j++ {
			var input int
			fmt.Println("matrix2[", i, "][", j, "] = ")
			fmt.Scanln(&input)
			matrix2[i][j] = input
		}
	}

	for i := 0; i < 2; i++ {
		for j := 0; j < 2; j++ {
			fmt.Println("matrix1[", i, "][", j, "] + ", "matrix2[", i, "][", j, "] = ", matrix1[i][j]+matrix2[i][j], "\b")
		}
	}

}*/

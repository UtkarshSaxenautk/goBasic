package main

/*import "fmt"

func main() {
	fmt.Println("Hlo google")

	arr := []string{"C++", "java", "Python"}
	for i, value := range arr {
		fmt.Println(i + 1)
		fmt.Println(value)
	}

	mymap := make(map[string]interface{})
	mymap["Name"] = "Utk"
	mymap["Age"] = 17
	for k, v := range mymap {
		fmt.Printf(" key: %s and value: %v ", k, v)
	}
}*/
